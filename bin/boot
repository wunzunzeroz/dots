#!/usr/bin/env bash
set -euo pipefail

die() {
  printf 'boot: %s\n' "$1" >&2
  exit 1
}

find_repo_root() {
  git rev-parse --show-toplevel 2>/dev/null || die "not a git repository"
}

main() {
  local root
  root="$(find_repo_root)"
  echo "$root"
}

main "$@"
